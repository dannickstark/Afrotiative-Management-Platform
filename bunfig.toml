[test]
# Bun skips .env.local when NODE_ENV=test; test-setup.ts preloads the real DB creds
# from .env.local (single source of truth) so integration tests hit the real Neon DB.
# Run DB-touching tests serially to avoid cross-test row contention.
preload = ["./test-setup.ts"]
